# 📖 Структура меню podkop_bot — справочник карточек

> Документ описывает экраны (карточки) Telegram-бота для управления [podkop](https://podkop.net).
> Для каждой карточки указан заголовок, строки данных и кнопки с описанием действия.
>
> 🇷🇺 **Интерфейс бота русский начиная с v0.18.x.** Все подписи ниже приведены так, как они
> выглядят на экране. Английскими остаются только технические термины: названия протоколов,
> поля UCI, `URLTest`, `SOCKS`, `DNS`, `YACD`, `sing-box`, `Zapret`, `ByeDPI`.
>
> Документ сверен с кодом **v0.19.9**. Значения в макетах — вымышленные: адреса взяты из
> диапазонов, зарезервированных под документацию (RFC 5737), версии и имена прокси даны
> для примера. Отметки **Plus**, **Forkop**, **NetShift** означают, что экран или кнопка
> есть только в этом варианте podkop.

---

## Навигационное дерево

Кнопки главного экрана расположены в два столбца — так они и показаны здесь.

```
🖥️ Управление Podkop  (/menu · main_menu)
├── 📊 Статус  cmd_status              │ 🌐 Прокси  (цель зависит от режима секции)
│   └── 🧭 Подробнее  cmd_runtime      │   ├── proxy_menu ....... Selector / URLTest / подписка
│       ├── 🧪 Диагностика             │   │   └── карточка прокси  px_view_N
│       ├── 📄 Файлы и журнал          │   ├── url_links_menu .... режим одной ссылки
│       └── 🔌 Закрыть соединения      │   ├── outbound_info ..... режим JSON-прокси
│           (только plus / forkop)     │   └── text_links_menu ... NetShift, текстовый режим
│
├── ⚙ Настройки  main_settings_menu   │ 🔄 Перезапустить  ask_reload_podkop
│   ├── ⚙ Настройки секции  section_settings
│   │   ├── Подключение · 🎯 Режим · Mixed-прокси · ✏ Порт
│   │   ├── 🎯 URLTest · 🔗 DNS-резолвер
│   │   ├── 🎯 Условия · 🔗 Через секцию      (только forkop)
│   │   ├── 🔬 Фильтры URLTest                (только plus, режим urltest)
│   │   └── включение секции и автозапуска
│   │       └── zapret_section_menu / byedpi_section_menu  (plus / forkop)
│   ├── 🌐 Общие  global_settings
│   │   └── интерфейс · интервал · QUIC · NTP в обход · DNS · YACD · контроль WAN
│   ├── 📎 Секции  sections_menu
│   └── 📄 Списки и маршруты  community_lists
│       └── готовые списки · домены · подсети · ➡ Через туннель · ↩ В обход ·
│           ✏ Мои домены · ✏ Мои подсети
│
├── 🤖 Настройки бота  bot_settings    │ 🛑 Остановить · 🟢 Запустить
│   ├── Подключение (Авто · SOCKS5 · Напрямую) · Интервал
│   ├── 🔗 Резервные SOCKS  fallback_socks_menu · 🧪 Проверить SOCKS
│   ├── ➕ Прокси бота · ➕ Привязать интерфейс
│   ├── Сообщать о запуске · о сбоях · Уведомлять всех · Контроль памяти
│   ├── Режим тишины · Ежедневный отчёт · Еженедельный отчёт
│   └── 👤 Администраторы  admins_menu
│
├── 🖧 Службы  cmd_server_instances                    (только plus / forkop)
│
└── 🔧 Обслуживание  cmd_maintenance
    ├── 🐶 Обновление Podkop · 🆕 Обновление бота
    ├── 📊 Отчёт за сутки · 📅 Отчёт за неделю
    ├── 📤 Загрузить скрипт
    ├── 🔄 Перезапустить бота
    └── 💀 Перезагрузить роутер

🧪 Диагностика  cmd_diagnostics        ← 📊 Статус → 🧭 Подробнее → 🧪 Диагностика
├── 🩺 Состояние туннеля  cmd_tunnel_health
├── 🔬 Проверить прокси · 🪺 Проверить задержки
├── 🌐 Проверка Podkop · 🧠 Проверка бота
└── 📋 Отчёт для поддержки

Постоянная reply-клавиатура (всегда внизу экрана):  [🏠 Меню] [📊 Статус]
```

---

## 🏠 Главное меню `/menu`

```
🖥️ Управление Podkop
Устройство: AX6000
Podkop: v0.7.14-r1 (Forkop) | Бот: v0.19.2
[Секция: main]             ← только при >1 секции
Активный прокси: LV-hysteria2 (main-1-out)
Подключение бота: Podkop (SOCKS5:192.168.2.1:2080)
```

| Кнопка | Действие |
|--------|----------|
| 📊 Статус | Открыть карточку системного статуса |
| 🌐 Прокси | Список прокси текущей секции. Подпись всегда одна, цель зависит от режима: `proxy_menu`, `url_links_menu` или `outbound_info` |
| ⚙ Настройки | Меню настроек podkop |
| 🔄 Перезапустить | Подтверждение → перезапуск podkop/sing-box |
| 🤖 Настройки бота | Подключение бота, уведомления, отчёты, администраторы |
| 🛑 Остановить / 🟢 Запустить | Остановить или запустить podkop (подпись меняется по факту) |
| 🖧 Службы | Живой статус серверных инстансов. Кнопка есть только на plus и forkop |
| 🔧 Обслуживание | Обновления, отчёты вручную, перезапуск бота, перезагрузка роутера |

**Строки карточки:**

| Строка | Что означает |
|--------|--------------|
| `Устройство` | Имя роутера (`/proc/sys/kernel/hostname`) |
| `Podkop` | Версия установленного пакета и в скобках — вариант (Podkop, NetShift, Podkop Plus, Forkop) |
| `Бот` | Версия запущенного бота |
| `Секция` | Активная UCI-секция (показывается только если секций больше одной) |
| `Активный прокси` | Читаемое имя активного прокси из Clash API (`#fragment` ссылки) плюс тег sing-box |
| `Подключение бота` | Каким путём бот сейчас ходит в Telegram (tier1 / tier2 / напрямую / аварийные IP) |

---
## 📊 Статус `cmd_status`

Первая строка — общий вердикт, ниже курсивом короткое пояснение к нему. Вердикт один из четырёх:

| Вердикт | Когда |
|---------|-------|
| ✅ **Podkop работает** | Служба поднята, туннель и Telegram доступны |
| ⚠️ **Работает с ограничениями** | Служба жива, но часть проверок не проходит |
| 🟡 **Бот использует резервный маршрут** | Основной туннель недоступен, бот держится на резерве |
| ❌ **Требуется действие** | Нужно вмешательство: служба лежит или конфиг сломан |

```
✅ Podkop работает
Туннель поднят, Telegram доступен.
────────────────────
🖥️ AX6000 | 5д 3ч 12м
🐧 OpenWrt 24.10.5
🌐 WAN: 198.51.100.24 — внешний 203.0.113.47
🏠 LAN: 192.168.2.1
🧠 0.18, 0.10, 0.08 | 💾 свободно 245 МБ
────────────────────
🐶 Forkop 0.7.14-r1 ✅
   автозапуск: ✅
📦 sing-box 1.11.15 ✅ | 28 МБ
🌐 🇳🇱 NL-vless-reality (main-1-out)
────────────────────
📨 Telegram: ✅ через Podkop (SOCKS5:192.168.2.1:2080)
🌐 Напрямую: ❌ недоступен · Резервный SOCKS: ✅ доступен
────────────────────
🛡️ Подключение бота: ✅ Podkop (SOCKS5:192.168.2.1:2080) 212 мс
🌐 DNS: doh — YACD: 🟢
```

| Строка | Что означает |
|--------|--------------|
| 🖥️ имя \| uptime | Имя роутера и время с последней загрузки |
| 🐧 | Версия OpenWrt |
| 🌐 WAN | Адрес WAN-интерфейса. Внешний IP дописывается только если отличается от WAN — то есть роутер за NAT |
| 🏠 LAN | Адрес LAN-интерфейса. Ниже автоматически добавляются VPN-интерфейсы, если есть |
| 🧠 \| 💾 | Средняя нагрузка за 1/5/15 минут и свободная память |
| 🐶 | Вариант podkop, его версия и состояние; отдельной строкой — автозапуск |
| 📦 | Версия sing-box, состояние и потребление памяти |
| 🌐 | Активный прокси с флагом страны и тегом sing-box |
| 📨 Telegram | Доступен ли Telegram тем путём, которым сейчас ходит бот |
| 🌐 Напрямую | Отдельная проверка Telegram в обход туннеля, плюс состояние резервного SOCKS |
| 🛡️ Подключение бота | Активный уровень транспорта и его задержка. Для tier4 и tier5 дописывается предупреждение |
| 🌐 DNS | Тип DNS секции и состояние YACD |

| Кнопка | Действие |
|--------|----------|
| 🧭 Подробнее | Открыть `cmd_runtime` |
| ♻️ Перезапустить Podkop | Подтверждение → перезапуск. Показывается, когда служба запущена |
| 🟢 Запустить | Показывается вместо предыдущей, когда служба остановлена |
| 🔃 Обновить · 🏠 Меню | Перечитать карточку · вернуться в главное меню |

---

## 🧭 Подробнее `cmd_runtime`

Подробности, которые не влезли в 📊 Статус: полная модель устройства, соединения и трафик из Clash API, активный прокси с задержкой, маршрут бота по уровням.

| Кнопка | Действие |
|--------|----------|
| 🧪 Диагностика | Открыть хаб `cmd_diagnostics` |
| 📄 Файлы и журнал | Открыть `cmd_files` |
| 🔌 Закрыть соединения | Сбросить все соединения через Clash API. **Только Plus и Forkop** |
| 🔄 Обновить · ← Назад · 🏠 Меню | |

---

## 🩺 Состояние туннеля `cmd_tunnel_health`

Состояние `sing-box` и `nftables`, режим секции, WAN и задержка по каждому уровню транспорта. Отдельно — две независимые проверки Telegram (напрямую и через туннель SOCKS5), блок активных прокси по секциям и доступность GitHub (`api.github.com`, `raw.githubusercontent.com`) напрямую и через SOCKS: по нему видно, получится ли обновиться из-под блокировок.

| Кнопка | Действие |
|--------|----------|
| 🔄 Обновить · ← Назад · 🏠 Меню | |

---

## 🧪 Диагностика `cmd_diagnostics`

Хаб проверок. Открывается из 🧭 Подробнее и из карточки прокси; «Назад» ведёт в `cmd_runtime`.

| Кнопка | Действие |
|--------|----------|
| 🩺 Состояние туннеля | `cmd_tunnel_health` |
| 🔬 Проверить прокси | Полная проверка через активный прокси, `ask_probe_outbound` |
| 🪺 Проверить задержки | Задержка по всем уровням транспорта, `cmd_upstream_health` |
| 🌐 Проверка Podkop | Штатные тесты podkop, `cmd_run_podkop_tests` |
| 🧠 Проверка бота | Внутренняя самопроверка бота, `cmd_run_internal_diag` |
| 📋 Отчёт для поддержки | Собрать архив с конфигом и логами, `ask_support_bundle` |
| ← Назад · 🏠 Меню | |

---

## 🔬 Проверить прокси `ask_probe_outbound`

Идёт через текущий активный прокси секции по Mixed Proxy и **не переключает** outbound на время проверки. Показывает: внешний IP и страну (ipapi.co, Cloudflare, Google), подсказку по стране от YouTube, доступность YouTube, Telegram API, ChatGPT, Claude.ai, Gemini и Discord, а затем замер скорости в два этапа — 32 KB ловит обрыв на старте, 1 MB меряет саму скорость.

Определение страны носит диагностический характер: разные источники могут расходиться.

| Кнопка | Действие |
|--------|----------|
| ✅ Запустить | Начать проверку (запуск подтверждается, потому что она занимает время) |
| ⚡ Проверить задержки · 🌐 Выбрать другой прокси | Быстрые действия по итогам |
| ← Отмена · ← Назад · 🏠 Меню | |

---

## 📄 Файлы и журнал `cmd_files`

| Кнопка | Действие |
|--------|----------|
| 📄 Конфигурация Podkop | Прислать `/etc/config/<вариант>` файлом |
| 📄 Конфигурация sing-box | Прислать `config.json` файлом |
| 📋 Журнал | Прислать системный журнал |
| ← Назад · 🏠 Меню | |

---

## 🌐 Прокси `proxy_menu`

Заголовок карточки зависит от режима секции — сама кнопка в главном меню всегда подписана `🌐 Прокси`, меняется только то, куда она ведёт:

| Режим секции | Заголовок карточки | Экран |
|--------------|--------------------|-------|
| `proxy:selector` | 🌐 **Прокси · Selector** | `proxy_menu` |
| `proxy:urltest` | 🎯 **Прокси · URLTest** | `proxy_menu` |
| `proxy:subscription` | 🔗 **Прокси подписки** (с уточнением · Selector или · URLTest) | `proxy_menu` |
| `proxy:url` | 🌐 **Прокси с одной ссылкой** | `url_links_menu` |
| `outbound` | ⚠ **Режим JSON-прокси** | `outbound_info` |
| `proxy:selector_text` / `proxy:urltest_text` | Текстовый режим NetShift | `text_links_menu` |

```
🎯 Прокси · URLTest [main]
Активный прокси: 🇳🇱 NL-vless-reality (main-1-out)

[0] ▶ 🇳🇱 NL-vless-reality | vless | 142 мс
[1] 🟢 🇩🇪 DE-vless-grpc | vless | 168 мс
[2] 🟡 🇫🇮 FI-hysteria2 | hysteria2 | 340 мс
[3] 🔴 🇺🇸 US-trojan-cdn | trojan | 910 мс

Выберите прокси, чтобы открыть сведения или сделать его активным.
```

Активный прокси помечен `▶` и выделен жирным, остальные — точкой по задержке. Кнопка каждого прокси повторяет разметку строки, поэтому номер со списком сверять не нужно. Значок `⊘` перед именем означает, что ссылка отключена.

| Цвет | Задержка | Вердикт в карточке прокси |
|------|----------|---------------------------|
| 🟢 | < 150 мс | Отличная |
| 🟢 | < 200 мс | Хорошая |
| 🟡 | < 500 мс | Допустимая |
| 🟠 | < 900 мс | Высокая, но допустимая |
| 🔴 | ≥ 900 мс | Очень высокая — лучше сменить прокси |

| Кнопка | Действие |
|--------|----------|
| `[N] ▶/🟢 <имя>` | Открыть карточку прокси `px_view_N` |
| ← Назад · 📄 N/M · Далее → | Пагинация, появляется если прокси больше восьми |
| 🪺 Автовыбор (URLTest) | Вернуть выбор автоматике. Только в режиме URLTest и если автовыбор сейчас не активен |
| ✏ URL подписки | Заменить URL подписки. Только в секциях с подпиской |
| ⚡ Проверить задержки | Замер по всем прокси; идёт отдельным сообщением, карточка остаётся на месте |
| ➕ Прокси · 🔄 Обновить | Добавить ссылку · перечитать список |
| 🧪 Диагностика · 🏠 Меню | |

---

## 📋 Карточка прокси `px_view_N`

Сведения об одном прокси: имя, протокол, адрес сервера, задержка с вердиктом из таблицы выше. Отсюда можно сделать прокси активным, проверить его отдельно или удалить.

Удаление идёт по паре `сервер:порт` — это работает одинаково для всех протоколов, в отличие от сопоставления по имени.

---

## 🌐 Прокси с одной ссылкой `url_links_menu`

Режим `proxy:url` — одна ссылка вместо списка.

| Кнопка | Действие |
|--------|----------|
| 🗑 `[N] <протокол>://…<хост>` | Удалить ссылку |
| ➕ Задать URL · 🔄 Обновить | |
| 🔬 Проверить прокси | Проверка через эту ссылку |
| ← Назад · 🏠 Меню | |

> При переключении в этот режим бот удерживает перезапуск podkop до тех пор, пока не получит ссылку — иначе туннель упал бы на пустом `proxy_string`.

---

## ⚠ Режим JSON-прокси `outbound_info`

Секция описана готовым JSON-outbound. Бот показывает состояние и не редактирует его: структура произвольная, и любая попытка править её из чата ломала бы конфиг. Правки — в LuCI.

| Кнопка | Действие |
|--------|----------|
| 🔄 Перезапустить · ← Назад · 🏠 Меню | |

---

## ⚙ Настройки `main_settings_menu`

| Кнопка | Действие |
|--------|----------|
| ⚙ Настройки секции | `section_settings` |
| 🌐 Общие | `global_settings` |
| 📎 Секции | `sections_menu` |
| 📄 Списки и маршруты | `community_lists` |
| ← Назад | |

---

## ⚙ Настройки секции `section_settings`

Набор кнопок зависит от варианта podkop и от действия секции. На Plus и Forkop, если действие секции — `zapret` или `byedpi`, вместо этого экрана открывается соответствующее меню DPI.

| Кнопка | Действие | Где есть |
|--------|----------|----------|
| Подключение: `<тип>` | `conn_type_menu` | везде |
| 🎯 Режим: `<режим>` | `proxy_mode_menu` | везде |
| `🟢/⚪` Mixed-прокси · ✏ Порт: N | Включение и порт Mixed Proxy | везде |
| 🎯 URLTest | Настройки URLTest секции | везде |
| 🔗 DNS-резолвер | `domain_resolver_settings` | везде |
| 🎯 Условия | Домены, IP, порты, устройства — `fk_conds` | **Forkop** |
| 🔗 Через секцию | Каскад detour — `fk_detour` | **Forkop** |
| ✏ Метка | Поле `label` секции | **Forkop** |
| `🟢/⚪` Сортировать по задержке | `sort_by_latency` | **Forkop**, действие `connection` |
| 🪜 Приоритеты | Priority Groups | **Forkop**, действие `connection` |
| 🔬 Фильтры URLTest | `urltest_filters_menu` | **Plus**, режим `urltest` |
| `🟢/⚪` Включить/отключить секцию | | везде |
| `🟢/⚪` Автозапуск | | везде |
| ← Назад | | |

> **Auto-assign порта.** Если `mixed_proxy_port` в UCI не задан, а Mixed Proxy включают из бота, podkop раньше падал с `jq: invalid JSON`. С `v0.14.1` бот сам подбирает первый свободный порт начиная с 2080.

### Подключение `conn_type_menu`

Варианты зависят от варианта podkop: `Туннелировать`, `В обход`, `Блокировка`, `Напрямую`, `Прокси`, `VPN`, а на Plus и Forkop дополнительно `Zapret`, `Zapret2`, `ByeDPI`.

### 🎯 Режим `proxy_mode_menu`

Переключение между `selector`, `urltest`, `url` и `outbound`. Текущий помечен ✅.

Если переключаться в URLTest с пустым списком ссылок, podkop не запустится — поэтому бот предупреждает заранее и предлагает перенести ссылки одной кнопкой: `🔄 Selector → URLTest (N)`. Обратный перенос — `🔄 URLTest → Selector (N)`.

---

## 🎯 URLTest `urltest_settings`

| Кнопка | Действие |
|--------|----------|
| ✏ Адрес проверки | URL, по которому sing-box меряет доступность. Проверяется строго: схема, непустой хост, отсутствие `?`/`#`/`@` в хосте, порт 1–65535 |
| ✏ Интервал | Как часто проверять. Формат `3m`, `180s`, `1h` |
| ✏ Допуск задержки | Максимальная разница задержки для переключения. Чем меньше, тем чаще URLTest будет менять прокси |
| 🌐 Прокси URLTest | Список ссылок URLTest |
| 🔄 Selector → URLTest (N) | Перенести ссылки из Selector |
| ← Назад | |

### Forkop: `fk_ut_menu`

На Forkop редактируется штатная дочерняя секция `urltest`, поэтому набор шире: помимо адреса проверки, интервала и допуска есть тумблер `Разрыв соединений` и раздел `🔬 Фильтры ротации`.

| Кнопка | Действие |
|--------|----------|
| ▶ Включить исключения · ⏸ Отключить фильтр | Переключение `filter_mode` |
| 🌍 Исключить страны по флагу | Показывает только страны, реально найденные среди текущих кандидатов, и число серверов у каждой |
| 🖥 Исключить прокси вручную | Постраничный выбор конкретных прокси |
| Использовать флаги 🇺🇳 | Явное переключение `detect_server_country` на `flag_emoji`, если в LuCI выбран другой способ |
| ← Назад без сохранения | Выйти, не применяя черновик |

> Первое добавленное исключение при `filter_mode=disabled` включает режим `exclude`. Режим `mixed` сохраняется — бот меняет только exclude-часть. При `include` запись блокируется: Forkop в этом режиме `exclude_*` не применяет вовсе, и «готово» было бы неправдой.

---

## 🔬 Фильтры URLTest `urltest_filters_menu` (Plus)

Отбирает прокси по стране, имени и регулярному выражению до того, как URLTest начнёт мерить задержку.

| Кнопка | Действие |
|--------|----------|
| Режим: `<значение>` | Цикл `выключен` → `исключение` → `только выбранные` → `mixed` |
| 🌍 `<способ>` | Определение страны: `по флагу` или `country_is` |
| 🌍 Исключить страны · 🌍 Только страны | Списки стран |
| 🖥 Исключить прокси · 🖥 Только прокси | Списки конкретных прокси |
| `🟢/⚪` Скрывать исключённые | Прятать отфильтрованные из списка |
| ← Назад | |

Список прокси берётся из штатных метаданных (`outboundMetadata.names`) — того же источника, что использует LuCI; Clash API остаётся запасным вариантом. Имена с флагами, пробелами и спецсимволами в callback не попадают: используется индекс.

> **Защита от параллельных правок (с 0.19.9).** Выбор собирается черновиком и применяется одной кнопкой, а не перезагружает sing-box на каждый клик. Перед применением бот сверяет сохранённую базу черновика с текущим UCI: если список успели изменить в LuCI или в другом сеансе, применение блокируется, устаревший черновик сбрасывается и предлагается открыть фильтр заново.

---

## 🎯 Условия `fk_conds` (Forkop)

Условия, по которым трафик попадает в секцию.

| Кнопка | Действие |
|--------|----------|
| Домены (N) · IP (N) · Порты (N) · Устройства (N) | Выбрать вид условий |
| ✏ Добавить · 🗑 Удалить № | Добавить строки или убрать по номеру |
| ← Назад · ← Отмена | |

Домены и IP Forkop хранит **одним многострочным полем** (`domain` с префиксами `full:`, `keyword:`, `regex:` внутри, и `ip_cidr`), а не списками UCI — бот пишет так же, как LuCI, иначе первое же сохранение в LuCI затёрло бы правки. Порты и адреса устройств остаются UCI-списками.

Комментарии в условиях (`#`, `//`) сохраняются: добавление дописывает строки к исходному тексту, а не пересобирает его из разобранных значений. Пробел внутри значения считается ошибкой ввода, а не склеивается: `keyword:google ai` отклоняется, а не превращается в `keyword:googleai`.

---

## 🔗 Через секцию `fk_detour` (Forkop)

Каскад: трафик секции уходит через другую секцию.

| Кнопка | Действие |
|--------|----------|
| `<текущая цель>` | Показывает, куда сейчас направлен detour |
| ✏ Выбрать секцию | Выбор целевой секции из списка |
| ← Назад | |

> Выбор передаётся индексом строки во временной таблице, привязанной к чату и сообщению. Раньше callback склеивал два UCI-идентификатора и ломался на именах секций с `_`, а общий файл таблицы перезаписывался следующим списком — старая кнопка могла выбрать не ту секцию, что была на её подписи.

---

## 🔗 DNS-резолвер секции `domain_resolver_settings`

| Кнопка | Действие |
|--------|----------|
| `<вкл/выкл>` | Включение резолвера для этой секции |
| Тип DNS: `<тип>` | Протокол |
| ✏ DNS-сервер | Адрес сервера |
| ← Назад | |

---

## 🌐 Общие `global_settings`

Настройки, общие для всего podkop, а не для отдельной секции: исходящий интерфейс, интервал обновления списков, тумблеры QUIC и «NTP в обход», «Через прокси», контроль WAN, а также переходы в 🔗 DNS и переключение YACD.

### 🔗 DNS `dns_settings`

| Кнопка | Действие |
|--------|----------|
| Протокол: `<UDP/DoH/DoT>` | `dns_proto_menu` |
| ✏ DNS-сервер · ✏ Bootstrap DNS | Адреса |
| ← Назад · 🏠 Меню | |

> **Forkop: DNS — список.** С 0.19.8 `dns_server` и `bootstrap_dns_server` редактируются как упорядоченный список: добавить, удалить, поднять или опустить. Верхний сервер основной, следующие резервные — бот больше не заменяет весь список одним значением. Ввод проверяется по тем же правилам, что и в апстриме: принимаются IPv4 и IPv6, домены, корректные порты и пути DoH.

### 🪺 Контроль WAN `badwan_details`

| Кнопка | Действие |
|--------|----------|
| `<вкл/выкл>` | Включение мониторинга |
| ✏ Интерфейсы | Список отслеживаемых интерфейсов |
| ✏ Задержка перезапуска | Пауза перед реакцией |
| ← Назад | |

---

## 📎 Секции `sections_menu`

Переключение активной секции podkop. Кнопка есть у каждой **неактивной** секции; переключение подтверждается (`✅ Да` / `← Отмена`).

| Случай | Что показывает |
|--------|----------------|
| Секций нет | Предупреждение, что конфиг может быть пуст или повреждён, и совет проверить его в LuCI |
| Секция одна | Пояснение, что переключаться не на что |
| Секций больше одной | Список остальных секций |

---

## 📄 Списки и маршруты `community_lists`

| Кнопка | Действие |
|--------|----------|
| ⚙ Готовые списки | Наборы вроде `russia_inside`, `telegram`, `twitter`, `cloudflare` |
| ⚙ Домены · ⚙ Подсети | Внешние списки по ссылке на `.lst` |
| ➡ Через туннель | Устройства, чей трафик целиком идёт в туннель |
| ↩ В обход | Устройства, которые ходят мимо туннеля |
| ✏ Мои домены · ✏ Мои подсети | Свои записи, постраничный редактор |
| ← Назад | |

В редакторах списков доступны `➕ Добавить`, `🗑 Удалить`, постраничная навигация и `📄 Скачать` — выгрузить список файлом.

---

## 🖧 Службы `cmd_server_instances` (Plus и Forkop)

Живой статус серверных инстансов из UCI. Поддержаны VLESS, VMess, Trojan, Shadowsocks, SOCKS, Hysteria2, MTProto и Tailscale.

Для каждого инстанса: протокол, порт, публичный хост, режим безопасности (Reality / TLS / none) с SNI и режим маршрутизации. Если доступен Clash API, добавляется число соединений и трафик.

| Значок | Состояние порта |
|--------|-----------------|
| 🟢 | Слушает по TCP и UDP |
| 🟡 | Включено в UCI, но порт не обнаружен |
| ⚫ | Выключено |

| Кнопка | Действие |
|--------|----------|
| ➕ Tailscale | Добавить узел Tailscale, `ts_add`. **Только Forkop** |
| `<вкл/выкл>` узла · выходной узел · принимать маршруты | Тумблеры на карточке узла |
| 🔄 Обновить · ← Назад | |

### ➕ Добавление узла Tailscale (Forkop)

Бот спрашивает адрес контрол-сервера, pre-auth ключ и нужен ли выходной узел, затем создаёт узел.

Требуется сборка sing-box `extended` либо собранная с тегом `with_tailscale`. Бот проверяет это **до** записи и просто не показывает кнопку там, где она не сработает.

Адрес контрол-сервера спрашивается всегда: по умолчанию он ведёт в облако Tailscale, но так же поддерживается свой сервер — Headscale, ionscale.

> **Узел создаётся выключенным, и это сделано намеренно.** Поднятие Tailscale-endpoint заставляет sing-box логиниться на контрол-сервер во время старта, а путь бота в Telegram идёт через тот же бэкенд: если логин не проходит, старт встаёт, и раньше это вешало бота целиком — снять можно было только по SSH. Поэтому создание узла — чисто конфигурационный шаг, не трогающий сеть, а включение вынесено в отдельное явное действие с предупреждением, что связь может пропасть.
>
> Pre-auth ключ удаляется из чата и **не пишется в syslog**: журнал живёт дольше, чем удалённое сообщение, поэтому в аудит уходит `<secret redacted>`.

---

## 🤖 Настройки бота `bot_settings`

| Кнопка | Действие |
|--------|----------|
| Подключение: `<Авто/SOCKS5/Напрямую>` | `ask_set_tr_menu`, с пояснением рисков и подтверждением |
| Интервал: N с | Интервал проверок, по кругу 60 → 30 → 120 → 300 |
| 🔗 Резервные SOCKS · 🧪 Проверить SOCKS | `fallback_socks_menu` и проверка всех уровней |
| ➕ Прокси бота / 🗑 Удалить прокси бота | Свой прокси третьим уровнем |
| ➕ Привязать интерфейс / 🗑 Отвязать интерфейс | Исходящий интерфейс бота |
| `🟢/⚪` Сообщать о запуске | Уведомление при старте бота |
| `🟢/⚪` Сообщать о сбоях | Уведомления watchdog |
| `🟢/⚪` Уведомлять всех | Рассылать всем `admin_ids`, а не только на главный `chat_id` |
| `🟢/⚪` Контроль памяти | Предупреждение при свободной RAM < 30 МБ |
| `🟢/⚪` Режим тишины · ⏰ Время | Часы, когда уведомления о сбоях молчат |
| `🟢/⚪` Ежедневный отчёт · ⏰ ЧЧ:ММ | |
| `🟢/⚪` Еженедельный отчёт · ⏰ `<день>` ЧЧ:ММ | |
| 👤 Администраторы | `admins_menu` |
| 🏠 Меню | |

### 🔗 Резервные SOCKS `fallback_socks_menu`

Формат записи: `socks5h://[user:pass@]host:port[#Имя]` — учётные данные и локальная мнемоника необязательны.

Пароль маскируется везде — в журнале, интерфейсе и уведомлениях (`user:***@`); если задана мнемоника, показывается она. Формат проверяется при вводе, неверный формат и неверный порт различаются, дубликаты по адресу отсеиваются. Пробелы в мнемонике заменяются на `_`, а в host, user и pass не допускаются.

Активный уровень помечен `◀ active`. Mixed-прокси других секций добавляются в резерв автоматически.

| Кнопка | Действие |
|--------|----------|
| 🗑 `[N] <прокси>` | Удалить запись |
| ➕ Добавить · 🔄 Обновить | |
| ← Назад · 🏠 Меню | |

### 👤 Администраторы `admins_menu`

| Кнопка | Действие |
|--------|----------|
| 🗑 `[N] <id>` | Удалить администратора, с подтверждением |
| ➕ Добавить администратора | Ввести User ID |
| `🟢/⚪` Анонимные администраторы | Разрешить анонимных админов в группах |
| 🤖 Пригласить бота | `@username`, ID, версия и инструкция для группы |
| 🔄 Обновить · ← Назад · 🏠 Меню | |

Основной администратор (`chat_id`) помечен 🔒 и не удаляется. Чтобы подключить группу, добавьте бота в неё и введите её `chat_id` кнопкой ➕.

---

## 🔧 Обслуживание `cmd_maintenance`

| Кнопка | Действие |
|--------|----------|
| 🐶 Обновление Podkop | `cmd_check_update` |
| 🆕 Обновление бота | `cmd_check_update_bot` |
| 📊 Отчёт за сутки | Прислать суточный отчёт сейчас |
| 📅 Отчёт за неделю | Прислать недельный отчёт сейчас |
| 📤 Загрузить скрипт | Установить бота из присланного `.sh` файла |
| 🔄 Перезапустить бота | С подтверждением |
| 💀 Перезагрузить роутер | Двойное подтверждение: кнопка, затем ввод `YES` |
| 🏠 Меню | |

> Ручные отчёты — снимки только для чтения: они не двигают автоматические окна суточного и недельного отчётов.

### 🆕 Обновление бота `cmd_check_update_bot`

Перед установкой бот показывает доступную версию, кратко что нового и ссылку «Полный список изменений».

| Кнопка | Действие |
|--------|----------|
| ✅ Обновить до vX.Y.Z | Установить новую версию |
| 🔄 Принудительно обновить | Переустановить текущую версию, если нужно применить патч |
| ← Отмена · ← Назад · 🏠 Меню | |

> Обновление сравнивает версии строго: равная версия обновлением не считается. Промежуточную сборку с более высоким номером кнопка не примет — такую нужно ставить через 📤 Загрузить скрипт, там сравнения версий нет.

### 📤 Загрузить скрипт

Присылаете `.sh` документом в чат. Бот проверяет shebang, наличие `BOT_VERSION` и синтаксис (`busybox ash -n`), делает резервную копию `.bak`, устанавливает и перезапускается. Способ обкатать патч без GitHub — и единственный вариант для роутеров, которым GitHub недоступен.

---

## 🔔 Автоматические уведомления (watchdog)

Приходят сами, без запроса. Каждое помечено префиксом `[имя роутера]`, чтобы в общей группе было видно, какой роутер жалуется.

| Событие | Сообщение |
|---------|-----------|
| sing-box остановлен | Туннель не работает, указан последний прокси |
| sing-box восстановлен | Туннель снова работает, указан активный прокси |
| Основной SOCKS недоступен | Бот переключился на резервный канал; показаны недоступный адрес и рабочий резерв |
| Основной SOCKS восстановлен | Бот вернулся на основной канал |
| Прокси переключён вручную | Было → стало |
| Смена прокси | 🔀 с дебаунсом 120 с: серия переключений группируется в одно сообщение, без спама при флаппинге URLTest |
| Мало памяти | Свободно меньше 30 МБ. Отбой при 40 МБ и выше, повтор не чаще раза в час |
| Деградация маршрута | Бот ушёл на «Напрямую» или на аварийные IP |
| Бот запущен | Имя роутера, версия podkop, маршрут |

Клавиатура `🏠 Меню | 📊 Статус` доступна и в момент уведомления.

**Режим тишины** глушит уведомления о сбоях в заданном диапазоне, включая переход через полночь. Отчёты за сутки и за неделю под него не попадают — они придут в любом случае.
